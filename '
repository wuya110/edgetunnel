行。只留核心。

正确点就这一条：

要改的核心

先缓存后续数据，等远端连接真正成功后，再把 socket 挂上去，再补发缓存。

为什么

你现在的问题是：

• 直连 socket 刚创建就先当成可用
• QX 后续 TLS 数据一来，先写进这个“可能马上失败”的 socket
• 然后它真失败了
• 再回落到 proxyip 时，关键握手数据已经丢了

正确时序

1. 收到首包，解析目标
2. 开始连接远端，但先别把这个 socket 宣布为可用
3. 这期间客户端再发来的数据，先缓存
4. 远端连接确认成功后

• 再设置 remoteConnWrapper.socket = remote
• 再把缓存数据补写进去

5. 然后再开始正常转发

一句话版本

不要让“可能马上死掉的 socket”提前上岗。

要加的东西

最少就这两个：

• connecting 状态
• pendingChunks 缓存队列

判定规则

• 已稳定连接 → 直接转发
• 连接中 / 回落中 → 先缓存
• 连接成功 → 挂 socket，补发缓存

这就是精简版。
如果你要，我下一条直接给你写成伪代码/补丁结构。